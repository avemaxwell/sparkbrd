// Image transform — passthrough (optimization disabled)
// All functions return the original URL unchanged.

export const tackCanvas  = (url: string) => url;
export const tackDetail  = (url: string) => url;
export const tackCollage = (url: string) => url;
export const tackThumb   = (url: string) => url;
export const tackMini    = (url: string) => url;
